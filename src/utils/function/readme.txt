this folder containt about function where can use for everything project

// 
exports const dispatch=(type,payload)=>return dispatch(type:type,payload:payload)
// so we can use

dispatch('SET_CURRENT_USER', user)