import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const SpacingFlex = ({spacing}) => {
  return <View style={{flex: spacing}} />;
};

export default SpacingFlex;

const styles = StyleSheet.create({});
